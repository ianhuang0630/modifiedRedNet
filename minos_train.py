import os
import numpy as np
import scipy

if __name__=='__main__':

    data_dir = '..'
    os.system('python ../../RedNet_train.py --data_dir {} --cuda --last-ckpt {} --summary_dir {} --epochs {}'.\
            format(
                # data_dir
                # last-ckpt dir
                # summary dir
                # epochs
                ))


